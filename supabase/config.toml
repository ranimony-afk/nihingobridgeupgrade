# Local Supabase project file. The app still uses Drizzle + DATABASE_URL.
# Start with: supabase start
# Then set SUPABASE_URL and SUPABASE_ANON_KEY from `supabase status`.

project_id = "nihongobridge"

[api]
enabled = true
port = 54321
schemas = ["public"]

[db]
port = 54322
major_version = 16

[studio]
enabled = true
port = 54323

[auth]
enabled = true
site_url = "http://localhost:3000"
