# A11 — DURABILITY MANIFEST

**Gate:** A11.5 (read-only durability / evidence / handoff audit)
**Date:** 2026-09-24
**Purpose:** freeze the surviving A11 evidence set as an immutable starting point for A12, after
repeated `.git` layer resets.

This manifest is **evidence only**. It modified no application behaviour. No A11 file, contract
section, test, schema or route was changed by this gate.

> **Content hashes are the durable identity of each artifact.** The `.git` object layer is not
> durable in this environment (see §3), so a path + line count + SHA-256 prefix is the reliable
> reference for A12, not a commit hash.

---

## 1. Repository identity at A11.5

| Item | Measured value |
| :--- | :--- |
| `HEAD` | `cfe565d58ec4f6902c9f00bba491827ab303a53d` |
| `origin/main` | `cfe565d58ec4f6902c9f00bba491827ab303a53d` |
| `git rev-list --left-right --count HEAD...origin/main` | `0  0` (no divergence) |
| Branch | `arena/01a0d21c-nihingobridgeupgrade` |
| `git log --oneline --decorate -20` | **one** commit only: `cfe565d (grafted, HEAD, origin/main, main) Merge pull request #10` |
| `git reflog --date=iso -30` | `clone` (12:21:52) + `checkout` (12:21:53) — nothing else |
| Total commit objects reachable | **1** |
| Working tree (collapsed) | 37 `??` + 31 `M` = 68 entries |
| Working tree (expanded) | 38 untracked files + 31 modified = 69 entries |

---

## 2. A11 evidence set — presence and content identity

| Artifact | Exists | Owner | State | Lines | SHA-256 (16) |
| :--- | :---: | :--- | :--- | ---: | :--- |
| `src/app/api/v1/mobile/dictionary/search/route.ts` | yes | **A11** | new, unmodified since A11 | 108 | `a9a06987bd9cf761` |
| `src/app/api/v1/mobile/dictionary/search/_lib.ts` | yes | **A11** | new, unmodified since A11 | 94 | `582d725faf96404f` |
| `tests/mobile-dictionary-search-api.test.ts` | yes | **A11** | DB-free contract/adapter suite | 722 | `a84b39fe09412717` |
| `tests/mobile-dictionary-search-api-live.test.ts` | yes | **A11** | live DB-backed suite | 208 | `029c4331ee90f433` |
| `docs/api/MOBILE-DICTIONARY-API-CONTRACT.md` | yes | A9→A10→**A11** | A9 body + §A10.1–18 + §A11.1–8 | 769 | `84da9e4a7dddc2c8` |
| `reports/gates/GATE-A11-MOBILE-DICTIONARY-SEARCH-API.md` | yes | **A11** | 31-section gate report | 773 | `c114d3d1fe1d79d0` |

Supporting contract artifacts that A11 depends on (not A11-authored, verified present):

| Artifact | Owner | Lines | SHA-256 (16) |
| :--- | :--- | ---: | :--- |
| `src/types/mobileDictionary.ts` | A9/A10 | 353 | `aece3af08cb9300d` |
| `tests/mobile-api-implementation-readiness.test.ts` | A10 | 360 | `b842abaa92788a29` |
| `tests/mobile-payload-design-freeze.test.ts` | A9 | 370 | `c682c4f3943cc469` |
| `tests/mobile-dictionary-script-contract.test.ts` | A7 | 235 | `204336b3bf968f63` |
| `tests/mobile-dictionary-payload-contract.test.ts` | A8 | 338 | `d0d4c0dd057e4747` |

No unrelated file is counted as an A11 deliverable.

---

## 3. Commit durability

| Check | Result |
| :--- | :--- |
| `git cat-file -t 694c0e2` (A11 first commit) | `fatal: Not a valid object name 694c0e2` |
| `git cat-file -t fa16e09` (A11 follow-up commit) | `fatal: Not a valid object name fa16e09` |
| `git cat-file -t 23ba543` (A10 commit, for contrast) | `fatal: Not a valid object name 23ba543` |

```text
A11 COMMIT OBJECT: NOT PRESENT IN CURRENT GIT LAYER
A11 FILE CONTENT:  SURVIVES
```

This is an **infrastructure property, not an A11 implementation failure**. The `.git` layer has
been re-cloned/reset on nine consecutive turns while working-tree files persisted. No destructive
operation was attempted to reconstruct the missing commits (no reset, clean, restore, checkout,
rebase, merge or cherry-pick), and none will be.

**Reference for A12: use the path + hash table above, not a commit hash.**

---

## 4. A10 contract integrity (unchanged by A11)

| Check | Result |
| :--- | :--- |
| `# A10 — IMPLEMENTATION-READINESS FREEZE` present | yes (line 328) |
| `## A10.x` anchors | **18** (A10.1 … A10.18) — complete |
| `## A11.x` anchors | **8** (A11.1 … A11.8) — additive only |
| A9 body intact | `# … **v1 (A9 FROZEN)**` at line 1; A9 markers present (`queryEcho` 3, `matchType` 4, `jlptStatus` 14, `hasMore` 15) |
| Frozen statements verbatim | `MOBILE-CANONICAL: \`id\`, and only \`id\`` (L425) · `RATE LIMIT:        none in v1` (L572) · `1…200 at the boundary; applied 1…100` (L372) · exact `hasMore` invariant (L32) |
| A11 rewritten A9/A10? | **no** — appended sections only; no historical decision was edited |

---

## 5. Implementation verification (read-only)

| Invariant | Evidence | Result |
| :--- | :--- | :--- |
| Route count under `src/app/api/v1` | `find … -name route.ts` | **1** — `mobile/dictionary/search/route.ts` |
| Method surface | `export` scan | `GET` + `dynamic` only; no POST/PUT/PATCH/DELETE |
| Detail route / catch-all | `find` for `[id]`, `[*]`, `[...*]` | **none** |
| Adapter ownership | `_lib.ts` exports `flattenGlosses`, `projectEntry`, `parseTargetLanguage` | pure functions; no matching/ranking/SQL |
| Closed projection | `projectEntry` object literal keys | `id headword reading romaji primaryGlosses jlptLevel jlptStatus isCommon kanjiCharacters` — exactly 9 |
| Row-spread scan | `...row` / `...entry` / `Object.assign` / `return row` / `return entries` / `databaseRow` / `$inferSelect` | **1 hit**, prose in a docblock describing the anti-pattern (`_lib.ts:19`); **0 in code** |
| Query sanitation | `sanitizeSearchQuery` reused (L51) | NUL-strip + trim; **no** `normalize`/`NFKC`/`toLowerCase`/transliteration in the A11 path |
| Script classification | `detectSearchScript` reused (L70); `ALL_SEARCH_SCRIPTS` = `empty, kanji, kana, japanese, romaji, mixed` | 6 values, unchanged; no `english`/`tamil`/`malayalam` |
| Target language | `SUPPORTED_LANGUAGES = ["en","ta","ml"]`; parsed then discarded | validated + **inert** |
| JLPT | item carries `jlptLevel` + `jlptStatus`; `jlptKnown` in the A11 path = **0** (the 1 repo hit is the A9 retirement note, `mobileDictionary.ts:96`) | tri-state preserved |
| Public ID | `id` verbatim; `ent_seq`/`entSeq` in `_lib.ts` = **0** | no alias, no migration |
| Pagination | `page` parsed = **0**; `parseLimit`/`parseOffset` (boundary 200 / applied 100 / offset 100 000) | flat offset/limit, applied echo |
| `hasMore` | `result.offset + entries.length < result.total` | exact, applied window |
| Error security | only messages are `"Query parameter 'q' is required"` and `"Dictionary search failed"`; `error.message` in the A11 path = **0** (the single `.message` match is a comment at L101); detail goes to `console.error` | no SQL/host/path/stack leakage |

---

## 6. Isolation and preservation

| Check | Command | Result |
| :--- | :--- | :--- |
| Web dictionary routes | `git diff --stat cfe565d -- src/app/api/dictionary` | **empty** (unmodified) |
| New route is additive | `git diff --stat cfe565d -- src/app/api/v1` | **empty** (path is new, nothing existing was edited) |
| Schema | `git diff cfe565d -- src/db/schema.ts` grep for `mobile\|v1\|dictionary_search` | **0**; the delta is 11 added comment lines / 1 removed (**pre-existing Gate A2** doc-comment about `provenanceType`/`ai_generated`) |
| Migrations | `git status --short -- drizzle` | **empty**; 4 migration files present and unmodified |
| Indexes | none created | — |
| Tatoeba | `git status --short -- data` | **empty**; `data/tatoeba/*.tar.bz2` untouched (5 archives present) |
| KanjiVG / Phase 14 | `src/services/knowledge/*` diffs contain only kanji-session lines (`projectEntry`/`v1/mobile` hits = **0**); `PHASE-14.5A-SCHEMA-NECESSITY.md` + `tests/kanjivg-etl.test.ts` are **pre-existing** modifications | untouched by A11 |

### Pre-existing modification classification (31 tracked files)

Every modified tracked file was classified by diff content. **A11 owns none of them.**

| Family | Files | Owner | Evidence |
| :--- | :--- | :--- | :--- |
| Search axis / ranking | `src/services/search/matcher.ts`, `search/types.ts`, `search/unifiedSearchService.ts` | **A7 / kanji session** | `search/types.ts` diff contains `-| "english"` removal + `ALL_SEARCH_SCRIPTS`; A11 markers (`projectEntry`, `v1/mobile`, `_lib`) = **0** |
| JLPT audit | `src/services/dataquality/jlptChecks.ts` | **Gate 14** | docstring corrections; `classifyJlptLevel` exists at `cfe565d` |
| CMS provenance | `src/services/cms/types.ts`, `cms/validation.ts`, `src/lib/cms-admin/payload.ts`, `src/components/admin/DictionaryList.tsx` | **Gate A2 / CMS** | provenance types + UI |
| Kanji graph | `src/services/knowledge/kanjiJmdictLinkage.ts`, `kanjiLexicalGraphService.ts` | **kanji session** | kanji lines only |
| ETL / corpus | `src/etl/dictionary/types.ts`, `src/etl/grammar/types.ts`, 9 corpus test files | **A6 / 14.x** | corpus tiering |
| Schema | `src/db/schema.ts` | **Gate A2** | **comment-only** (verified above) |
| Docs | `docs/architecture/*.md` (3), `docs/architecture/MOBILE-DICTIONARY-API-CONTRACT.md` | **A7/A8/A9** | banner + corrections |
| Reports | `reports/gates/PHASE-14.5A-SCHEMA-NECESSITY.md` | **14.5A** | pre-existing |
| CI / config | `.github/workflows/ci.yml`, `.gitignore` | **A6 / session** | corpus tier + ignore rules |

**Uncertain files: 0.**

### Untracked inventory reconciliation

38 untracked files, every one mapping to a known gate family (A1–A11, kanji session, 14.x). No
build artifacts, no stray output, no `.next`/`*.tsbuildinfo`/logs (all gitignored).

Arithmetic that proves nothing was added between gates:

```text
A11 Gate 0  (before A11 work):  33 collapsed ??
A11 additions (4 collapsed entries): src/app/api/v1/, 2 test files, A11 report
                                 -----
current (collapsed):            37 ??
```

---

## 7. Validation results

| Validation | Result |
| :--- | :--- |
| A11 suites (route + adapter + live) | **49 passed / 0 failed** (42 DB-free + 7 live; 19.14 s) |
| Mobile + web contract suites | **118 passed** (payload 14, script 35, design-freeze 18, readiness 20, dictionary-kanji routes 31) |
| Full regression | **56 files / 1124 passed / 0 failed / 55 skipped / 1179 total** |
| Baseline (A10) | 54 files / 1075 passed / 0 failed / 55 skipped / 1130 total |
| A11 final (pre-A11.5) | 56 files / 1124 passed / 0 failed / 55 skipped / 1179 total |
| New failures | **0** |
| New skips | **0** |
| Corpus skip set | **55**, byte-identical across the same nine files |
| Typecheck (`npx tsc --noEmit`) | **clean** |
| Lint (`npm run lint`) | **0 errors / 4 warnings** (unchanged) |
| Drizzle (`npx drizzle-kit check`) | **OK** |
| Build (`npm run build`) | **exit 0**; route registered as dynamic |
| Package integrity | `package.json` + `package-lock.json` clean before **and** after `npm ci` (341 entries) |

**Count correction for the record:** the A11.5 brief cites the A11 result as
`56 / 1122 / 0 / 55 / 1177`. After the A11 follow-up added two explicit pagination boundary tests,
the measured and reported figures are **`56 / 1124 / 0 / 55 / 1179`** — i.e. +2 passed and +2 total.
The A11 report and the contract carry the corrected numbers; the current re-run reproduces them
exactly.

---

## 8. Production safety

| Check | Evidence | Result |
| :--- | :--- | :--- |
| `.env` / `.env.local` | `ls -la .env*` → only `**`.env.example**` (tracked template) | none present |
| `DATABASE_URL` in shell | unset at audit start; only ever set to `127.0.0.1` loopback for this gate | loopback only |
| `.vercel` directory | absent | none |
| Vercel/Supabase CLI invocation | not in `package.json` scripts; no CLI call made | none |
| Listening sockets | `ss -lnt` → `:111`, `:49983`, `:22` only (plus the disposable PGlite `:5432` during validation) | no production endpoint |
| Stray processes | `ps` → no `next`/`tsx` process outside the declared disposable DB | none |

```text
VERCEL PRODUCTION CONTACTED:   NO
SUPABASE PRODUCTION CONTACTED: NO
PRODUCTION DATABASE READ:      NO
PRODUCTION DATABASE WRITTEN:   NO
PRODUCTION ENVIRONMENT CHANGED:NO
PRODUCTION DEPLOYMENT:         NO
```

`@supabase/ssr` and `@supabase/supabase-js` appear in `package.json` as application dependencies
(they back the auth layer). No A11 or A11.5 code path calls them for the dictionary endpoint, and
no network call to a Supabase host was made by this gate.

---

## 9. A12 starting conditions

```text
START FROM:  the surviving working tree + the frozen A10 contract + the verified A11
             implementation + reports/gates/A11-TO-A12-HANDOFF.md
DO NOT START FROM: an assumption that 694c0e2 (or any A11/A10 commit) exists

A11 IMPLEMENTATION: COMPLETE and verified at the hashes in §2
A11 REGRESSION:     green (56 / 1124 / 0 / 55 / 1179)
A11 FILES:          preserved (6 artifacts, hashes recorded)
A10 CONTRACT:       frozen and intact (18 × A10.x + 8 × A11.x anchors)
SECURITY BOUNDARY:  preserved (public, read-only, no identity, closed projection, safe errors)
A11.5 ARTIFACTS:    this manifest, reports/gates/A11-TO-A12-HANDOFF.md,
                    reports/gates/GATE-A11.5-DURABILITY-TRANSITION.md
A12 IMPLEMENTATION: NOT STARTED (no detail route, no rate limiting, no auth, no localization,
                    no index, no schema change — verified by route inventory and diff audit)
```

**A11.5 verdict: PASS WITH CONDITIONS** — the implementation and evidence are intact; the
outstanding conditions are infrastructure durability (commit objects absent) and the already-frozen
deployment/security deferrals (D-13, D-14), which A11 and A11.5 were both forbidden to implement.

---

## 10. Re-verification (second A11.5 pass)

A11.5 was re-run after another git-layer reset. Every figure below was **re-measured**, not carried:

| Check | First pass | Re-verification | Drift |
| :--- | :--- | :--- | :--- |
| HEAD / origin / distance | `cfe565d` / `cfe565d` / `0 0` | `cfe565d` / `cfe565d` / `0 0` | none |
| Reachable commits | 1 | 1 (`cfe565d`, grafted) | none |
| `fa16e09` / `694c0e2` / `b1246f1` | absent | **absent** | none |
| Working tree | 37 `??` + 31 `M` | **40 `??` + 31 `M`** (+3 = the A11.5 artifacts) | explained |
| A11 artifact hashes (7 files) | recorded §2 | **byte-identical, all 7** | **none** |
| A11 suites | 49 passed | **49 passed** | none |
| Full regression | 56 / 1124 / 0 / 55 / 1179 | **56 / 1124 / 0 / 55 / 1179** | none |
| Corpus skip set | 55 across 9 files | **55 across the same 9 files** | none |
| Typecheck | clean | **clean** | none |
| Lint | 0 err / 4 warn | **0 err / 4 warn** | none |
| Drizzle check | OK | **OK** | none |
| Build | exit 0 | **exit 0** (route registered) | none |
| Package files | clean | **clean before and after `npm ci`** | none |
| Uncertain files | 0 | **0** | none |

**Filename correction:** the manifest was renamed from `A11-DURABILITY-MANIFEST.md` to
`A11.5-DURABILITY-MANIFEST.md` to match the gate's required artifact path, and the cross-references
in the handoff and transition report were updated. No content of the audit was altered.

**Arithmetic closing the untracked set** (the check that no file appeared between gates):

```text
A11 Gate 0 (before A11 work):  33 collapsed ??
+ A11 created:                  4  (src/app/api/v1/, 2 test suites, A11 report)
+ A11.5 created:                3  (manifest, handoff, transition report)
                              ----
= 40 collapsed ??  (measured: 40)   → 0 unexplained entries
```
